doctype html
head
  title= title
  link(rel="stylesheet" href="/style.css")
  meta(name="viewport" content="width=device-width, initial-scale=1")
  meta(charset='utf-8')
body
  h1
    a(href="/")= siteName
  if message
    h2(id="message")= message
  block content
  script(src="/script.js")